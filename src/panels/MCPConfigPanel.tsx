import React from "react";

const MCPConfigPanel: React.FC = () => {
  return (
    <div className="chatbox-main-panel chatbox-mcp-config-panel">
      <h3>MCP 配置</h3>
      <div>（此处可添加 MCP 相关配置表单）</div>
    </div>
  );
};

export default MCPConfigPanel;
